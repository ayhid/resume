---
phase: 01-reviewable-baseline-and-safe-delivery
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - .claude/CLAUDE.md
autonomous: true
requirements: [OPS-02, OPS-03, OPS-04]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A visitor loading https://ayoub-hidri.dev/ is served the v2 page, not the previous online CV."
    - "The seven commits still held locally — plan `01-03`'s six-slice rewrite series plus this plan's CLAUDE.md correction — reach `origin/main` in a single push, producing exactly one production deployment rather than one per commit. Plans `01-01` and `01-02` pushed the four delivery commits before them, so the phase's eleven commits reach the origin in three pushes and no half-rewritten state ever reaches a runner or a visitor."
    - "What is served after the push is the D-06 manifest only: `/README.md`, `/og-image.html`, `/specs/design.md` and `/.planning/STATE.md` all return 404 while `specs/` and `.planning/` sit on the origin."
    - "`/`, `/robots.txt`, `/sitemap.xml`, `/og-image.png` and `/CNAME` return 200."
    - "The publishing source is still the Actions workflow after the largest push of the phase: no `pages build and deployment` run is created."
    - "`.claude/CLAUDE.md` no longer states that the whole repository root is published or that deleting the `CNAME` file breaks the domain — both became false when the allowlist and the workflow publishing source landed."
    - "`git status --porcelain` produces no output and local `main` and `origin/main` are level."
  prohibitions:
    - "The rewrite must not be edited while landing it; the published page is byte-identical to what plan `01-03` committed."
    - "The series must not be pushed commit by commit — one push, one deployment."
    - "No force-push to `main` under any circumstances."
    - "The snapshot branch must not be pushed to any remote."
    - "specs/, README.md, .planning/ and .github/ must not ship in the published artifact."
    - "og-image.html must not be reachable in production."
    - "The deploy job must not be reachable from a pull-request-triggered run."
    - "No commit may carry an AI attribution trailer or generation footer."
  artifacts:
    - path: ".claude/CLAUDE.md"
      provides: "Architectural constraints corrected to describe the allowlist artifact and the settings-resident custom domain"
      contains: "_site"
      min_lines: 320
  key_links:
    - from: ".claude/CLAUDE.md"
      to: ".github/scripts/stage-site.sh"
      via: "the corrected deployment constraint points future agents at the allowlist as the single source of truth for what ships"
      pattern: "stage-site\\.sh"
    - from: "origin/main"
      to: "https://ayoub-hidri.dev/"
      via: "one push fires one Deploy to GitHub Pages run whose deploy job publishes the staged artifact"
      pattern: "Deploy to GitHub Pages"
---

<objective>
Land the phase. Correct the two architectural claims in `.claude/CLAUDE.md` that this phase made
false, then push the whole series once and prove the live domain serves the v2 page and nothing
but the manifest.

Purpose: ROADMAP criteria 3 and 4. Everything before this plan was rehearsal — the pipeline was
proven on a change whose payload was the pipeline itself, and the rewrite was sliced but never
shipped. This is the plan where a real visitor sees a different site.

Output: a corrected `.claude/CLAUDE.md`, one push, one deployment, and a live status matrix that
matches the expected column.
</objective>

<reversibility>
| Decision | Rating | Rationale |
|---|---|---|
| The commit series lands directly on public `main` (D-03) | **costly** | Carried forward from `01-CONTEXT.md`, where the developer chose it at discuss time. Once pushed, rewriting a commit boundary means a force-push to a public deployed branch. That cost is why plan `01-03` settled the boundaries locally and asserted byte-identity before this plan runs. No checkpoint is inserted: the door was chosen deliberately, and re-asking a decided question is checkpoint fatigue, not diligence. |
| Correcting `.claude/CLAUDE.md` in this phase | reversible | A documentation edit in a tracked file. |
| Deleting `wip/v2-snapshot` after the live check passes | reversible | Recoverable from the reflog; deletion is deliberately ordered after the live verification rather than before it. |

No `one-way` decision appears in this plan, so no `checkpoint:decision` is inserted.
</reversibility>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reviewable-baseline-and-safe-delivery/01-CONTEXT.md
@.planning/phases/01-reviewable-baseline-and-safe-delivery/01-RESEARCH.md
@.planning/phases/01-reviewable-baseline-and-safe-delivery/01-01-SUMMARY.md
@.planning/phases/01-reviewable-baseline-and-safe-delivery/01-03-SUMMARY.md
@.claude/CLAUDE.md
</context>

<edge_assumptions>
| Requirement | Probe category | Disposition |
|---|---|---|
| OPS-04 | adjacency | **Resolved** into a `must_haves.truths` predicate on plan `01-01` and re-asserted here after the largest push of the phase: exactly one publisher targets the site, proven by the newest `event: dynamic` run timestamp being unchanged across the push. |
| OPS-04 | empty | **Resolved** into a `must_haves.truths` predicate on plan `01-01` and observed on plan `01-02`, where a push touching no published asset republished an identical artifact. |
| OPS-03 | unclassified | **Flagged assumption, unresolved.** Carried on plan `01-03`; the human readability judgement lands in plan `01-05`. |

The full six-row probe report is recorded in plan `01-01` §edge_assumptions. Nothing is dropped.
</edge_assumptions>

<artifacts_this_phase_produces>
| Symbol | Kind | Introduced by |
|---|---|---|
| `.github/scripts/stage-site.sh` | file | plan `01-01` |
| `.github/scripts/verify_site.py` | file | plan `01-02` |
| `verify`, `deploy` | workflow job names | plan `01-01` |
| `Stage production artifact`, `Verify artifact contents` | workflow step names | plans `01-01`, `01-02` |
| `wip/v2-snapshot` | local-only git branch, deleted at the end of this plan | plan `01-03` |
| `_site/` | CI staging directory (gitignored) | plan `01-01` |
| `chore/pr-gate-probe` | throwaway git branch | plan `01-05` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Correct the two architectural constraints this phase made false</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - `.claude/CLAUDE.md` — read lines 60–80 and 260–280; the stale claims sit at lines 66, 77, 266, 273 and 274
    - `.github/workflows/deploy.yml` — the shipped two-job structure, which is what the corrected text must describe
    - `.github/scripts/stage-site.sh` — the manifest the corrected text points at
    - `.planning/phases/01-reviewable-baseline-and-safe-delivery/01-RESEARCH.md` — §Pitfall 5, §State of the Art, §Project Constraints (from CLAUDE.md), §Open Questions item 4
  </read_first>
  <action>
    Two architectural constraints in `.claude/CLAUDE.md` were true when written and are false as of
    plan `01-01`. Every future agent reads this file, so leaving them is a defect with a long tail.
    Edit only what became untrue; do not restructure the document, do not reflow untouched
    paragraphs, and do not remove either constraint heading.

    **The deployment scope claim, at lines 77, 266 and 273.** Today these three places state that
    the whole repository root is uploaded as the artifact and therefore that every committed root
    file — naming `og-image.html` and `README.md` explicitly — ships to production. Rewrite each to
    describe what now happens: the workflow stages an allowlisted set into `_site/` via
    `.github/scripts/stage-site.sh` and uploads that directory, so only `index.html`, `CNAME`,
    `robots.txt`, `sitemap.xml`, `og-image.png` and, when it exists, `en/` are published, while
    `og-image.html`, `README.md`, `specs/` and `.planning/` are not. Keep the operational warning
    that made the original line useful, inverted to match the new default: a new production asset
    is not published until it is added to the manifest in `stage-site.sh`. At line 77 also correct
    the pipeline description — `concurrency` is now declared per job, not workflow-wide, and the
    workflow has two jobs where the pull-request event reaches only the verify job. At line 266
    correct the responsibility chain to include the staging step between `configure-pages` and
    `upload-pages-artifact`, and to name the verification step.

    **The custom domain claim, at lines 66 and 274.** Today these state that the `CNAME` file binds
    the domain and that removing that file would take the domain down. Under a workflow publishing source GitHub
    creates no `CNAME` file and ignores any existing one; the binding lives in the Pages settings,
    which hold `ayoub-hidri.dev` with HTTPS enforced. Rewrite both to say that the domain is bound
    in repository settings, that the `CNAME` file is retained in the manifest per D-06 as
    belt-and-braces rather than as the binding, and that the file is now fetchable at `/CNAME` as a
    harmless consequence of shipping it.

    Add no new claim that is not verified by something this phase shipped. Preserve the file's
    Markdown conventions, use no trailing whitespace, and keep the terminating newline.

    Commit alone, in the repository's Conventional Commits style with no AI attribution trailer and
    no generation footer: `docs(claude): correct the deployment scope and custom domain constraints`.

    Do not push in this task.
  </action>
  <verify>
    <automated>grep -c '_site' .claude/CLAUDE.md && grep -c 'stage-site.sh' .claude/CLAUDE.md && git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'stage-site.sh' .claude/CLAUDE.md` is at least `2`
    - `grep -c '_site' .claude/CLAUDE.md` is at least `2`
    - `grep -ci 'whole repo root is uploaded\|every committed file at the root is published' .claude/CLAUDE.md` outputs `0`
    - `grep -ci 'deleting it breaks the domain' .claude/CLAUDE.md` outputs `0`
    - `grep -c "path: '\.'" .claude/CLAUDE.md` outputs `0`
    - `grep -c 'og-image.html' .claude/CLAUDE.md` is at least `1` and every remaining occurrence describes it as excluded from the published artifact
    - `git show --stat --format= HEAD | grep -c 'CLAUDE.md'` outputs `1` and no other file appears in that commit
    - `git log -1 --format='%s'` outputs `docs(claude): correct the deployment scope and custom domain constraints`
    - `git log -1 --format='%B' | grep -ci 'co-authored-by\|generated with'` outputs `0`
    - `grep -c '[[:space:]]$' .claude/CLAUDE.md` outputs `0` and `tail -c 1 .claude/CLAUDE.md | od -An -c | tr -d ' '` outputs `\n`
    - `git status --porcelain` produces no output
    - `git rev-list --left-right --count origin/main...main | cut -f2 | tr -d ' '` outputs `7` — plan `01-03`'s six slice commits plus this one, all still unpushed
  </acceptance_criteria>
  <done>The two constraints every future agent reads now describe the pipeline that actually exists, and the correction is its own commit at the end of the series.</done>
</task>

<task type="auto">
  <name>Task 2: Push once, and prove the live domain serves v2 and nothing else</name>
  <files>none — this task pushes existing commits and asserts against the live domain</files>
  <precondition>`git diff --quiet wip/v2-snapshot -- index.html README.md` exits 0 and `git status --porcelain` produces no output — the reconstructed series is byte-identical to the rewrite and the tree is clean.</precondition>
  <reversibility rating="costly">This is the push D-03 chose: the series lands on public `main`, and after it a boundary correction would mean a force-push to a deployed branch.</reversibility>
  <read_first>
    - `.planning/phases/01-reviewable-baseline-and-safe-delivery/01-01-SUMMARY.md` — the recorded baseline status matrix and the `event: dynamic` timestamp this task compares against
    - `.planning/phases/01-reviewable-baseline-and-safe-delivery/01-RESEARCH.md` — §Pitfall 2, §Pitfall 1, §Validation Architecture
    - `.planning/phases/01-reviewable-baseline-and-safe-delivery/01-VALIDATION.md` — §Live `curl` Matrix
  </read_first>
  <action>
    Assert the precondition first and stop if it fails. Then push `main` once — seven commits in a
    single push. Not once per commit: every push to `main` is a production deploy, and D-02
    deliberately allows the first four of plan `01-03`'s six slice commits to render imperfectly. A
    single push fires exactly one workflow run at the tip commit, so those intermediate states never
    reach the runner and never reach a visitor. Never use `--force`, and never push
    `wip/v2-snapshot`.

    Watch the run to completion with `gh run watch`. Confirm the `verify` job passed, the `deploy`
    job ran, and the run's conclusion is `success`. Then confirm exactly one `Deploy to GitHub
    Pages` push run was created for this push, and that the newest `event: dynamic` run timestamp
    is identical to the one recorded in plan `01-01` — the legacy builder must still be silent
    after the largest push of the phase.

    Re-measure the full live status matrix and compare it against the baseline recorded in plan
    `01-01`. The four manifest URLs and `/` must be 200; `/CNAME` must now be 200; `/README.md`,
    `/og-image.html`, `/specs/design.md` and `/.planning/STATE.md` must be 404. The last two matter
    most: `specs/` and `.planning/` are on the origin now, and they still do not resolve. That is
    the whole of OPS-02, observed rather than asserted.

    Then confirm the served page is the rewrite and not the previous CV. Fetch `/` and check that
    it carries the bilingual language-block markup the rewrite introduced, and that the previous
    page's CDN stylesheet tag is gone. Both discriminators were verified at planning time: the
    committed page before this phase carried the CDN tag and no language block, and the rewrite
    carries four language-block attribute occurrences and no CDN tag.

    Finally, once the live checks have passed and only then, delete the local `wip/v2-snapshot`
    branch. It stayed alive through the push on purpose — it is the recovery net for exactly the
    window this task opens. Note in the SUMMARY that the reflog retains it.

    Record the after-matrix in the SUMMARY next to the before-matrix from plan `01-01`, so
    criterion 3 reads as a before/after rather than as a claim.
  </action>
  <verify>
    <automated>curl -s -L https://ayoub-hidri.dev/ | grep -c 'data-lang-block' && curl -s -o /dev/null -w '%{http_code}\n' -L https://ayoub-hidri.dev/README.md && gh run list --event push --limit 1 --json conclusion --jq '.[0].conclusion'</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --left-right --count origin/main...main | tr -s '\t' ' '` outputs `0 0`
    - `git status --porcelain` produces no output
    - `git reflog --date=iso main | grep -ci 'forced-update'` outputs `0` — no force-push occurred
    - `git ls-remote --exit-code --heads origin wip/v2-snapshot; echo $?` outputs `2` — the snapshot was never pushed
    - `git rev-parse --verify wip/v2-snapshot 2>/dev/null; echo $?` outputs `128` — the snapshot was deleted, after the live checks passed
    - `gh run list --event push --limit 1 --json conclusion,name --jq '.[0].conclusion + " " + .[0].name'` outputs `success Deploy to GitHub Pages`
    - `gh run list --event dynamic --limit 1 --json createdAt --jq '.[0].createdAt'` outputs the identical timestamp recorded in plan `01-01`
    - `curl -s -L https://ayoub-hidri.dev/ | grep -c 'data-lang-block'` outputs `4`
    - `curl -s -L https://ayoub-hidri.dev/ | grep -c 'cdn.tailwindcss.com'` outputs `0`
    - `curl -s -L https://ayoub-hidri.dev/ | grep -c 'hero-title-fr'` outputs `1`
    - `curl -s -o /dev/null -w '%{http_code}' -L https://ayoub-hidri.dev/README.md` outputs `404`
    - `curl -s -o /dev/null -w '%{http_code}' -L https://ayoub-hidri.dev/og-image.html` outputs `404`
    - `curl -s -o /dev/null -w '%{http_code}' -L https://ayoub-hidri.dev/specs/design.md` outputs `404`
    - `curl -s -o /dev/null -w '%{http_code}' -L https://ayoub-hidri.dev/.planning/STATE.md` outputs `404`
    - `curl -s -o /dev/null -w '%{http_code}' -L https://ayoub-hidri.dev/` outputs `200`, and `/robots.txt`, `/sitemap.xml`, `/og-image.png`, `/CNAME` each output `200`
    - `curl -s -o /dev/null -w '%{http_code}' -L https://ayoub-hidri.dev/en/` outputs `404` — reserved for Phase 10
    - `gh api repos/ayhid/resume/pages --jq '.build_type + " " + .cname + " " + .status'` outputs `workflow ayoub-hidri.dev built`
    - `git log --format='%s' <PHASE_BASE>..origin/main | wc -l | tr -d ' '` outputs `11` — the phase's whole commit series is now on the origin, where `<PHASE_BASE>` is the pre-phase `origin/main` SHA recorded in `01-01-SUMMARY.md`
    - `git log --format='%s' <PHASE_BASE>..origin/main | grep -c '^ci(deploy): '` outputs `3`; `... | grep -c '^chore: '` outputs `1`; `... | grep -c '^feat('` outputs `5`; `... | grep -c '^docs('` outputs `2`
    - `git log --reverse --format='%s' <PHASE_BASE>..origin/main | head -1` outputs `ci(deploy): add production allowlist staging script` — the deploy workflow change is the oldest commit of the phase, which is D-04 asserted against the commit D-04 names
  </acceptance_criteria>
  <done>One push produced one deployment; the live domain serves the v2 page; `specs/`, `.planning/`, `README.md` and `og-image.html` are on the origin and unreachable in production; the legacy builder stayed silent; and the snapshot branch has been retired now that its content is published.</done>
</task>

</tasks>

<threat_model>
ASVS level 1. Blocking severity: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| local `main` → public `main` | Crossed for the last time in this plan: seven commits — plan `01-03`'s six-slice rewrite series plus the CLAUDE.md correction — reach the origin in one push. `specs/` and `.planning/` arrived on the origin earlier, with the phase's first push in plan `01-01`, which is exactly why the allowlist and the publishing-source switch had to land before that push. |
| repository content → public web | The push is a production publish; the allowlist is the only thing between the origin and the domain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information disclosure | published artifact once `specs/` and `.planning/` are on the origin | critical | mitigate | The publishing source is `workflow` and the artifact is the D-06 allowlist, both landed and observed before this push. Proven here by 404 on `/specs/design.md` and `/.planning/STATE.md` while those directories exist on the origin — the strongest form of the OPS-02 assertion available. |
| T-01-16 | Denial of service | serving a half-rewritten page to real visitors | high | mitigate | One push, one deployment at the tip commit. The four intermediate states permitted by D-02 exist only in history and never reach the runner. |
| T-01-14 | Repudiation / tampering | force-push to a public deployed branch | medium | mitigate | Boundaries were settled locally in plan `01-03` and asserted byte-identical before the push; `--force` is prohibited and the reflog is checked for a forced update. |
| T-01-17 | Spoofing | custom domain or certificate lost across the largest push | medium | mitigate | `gh api repos/ayhid/resume/pages` is re-read after the deployment and must still report `ayoub-hidri.dev` with status `built`; the live 200 on `/` over HTTPS is the end-to-end confirmation. |
| T-01-18 | Tampering | a second publisher racing the Actions deployment | high | mitigate | The newest `event: dynamic` run timestamp is compared against the value recorded before the very first push of the phase; any legacy-builder run appearing here means the source switch regressed. |
| T-01-08 | Information disclosure | Mixpanel project token in public git history at `0650811` | high | accept | Developer-confirmed deferral recorded in `01-CONTEXT.md` §Deferred Ideas and `.planning/STATE.md` §Blockers/Concerns. This push makes the token no *more* reachable — it was already in public history — but the exposure remains live and unmitigated. Re-raise at milestone close. |
</threat_model>

<verification>
- Exactly one `Deploy to GitHub Pages` push run for this push, conclusion `success`
- No new `event: dynamic` run
- The live status matrix matches the expected column, including 404 on `/specs/design.md` with `specs/` present on the origin
- The served page carries the rewrite's bilingual markup and none of the previous page's CDN stylesheet tag
- `git status --porcelain` produces no output; local and remote `main` are level
- No force-push in the reflog; `wip/v2-snapshot` never reached the origin and is now deleted locally
</verification>

<success_criteria>
- ROADMAP criterion 3: merging to `main` publishes, and what is served contains only production assets
- ROADMAP criterion 4: a visitor loading https://ayoub-hidri.dev/ sees the v2 page
- OPS-03 completes: the series is in public history and the working tree is clean
- OPS-04: deployment to production happened on the push to `main`, through the Actions workflow only
</success_criteria>

<output>
Create `.planning/phases/01-reviewable-baseline-and-safe-delivery/01-04-SUMMARY.md` when done,
containing the before and after status matrices side by side, the deployment run URL, and the
`PHASE_BASE` SHA carried forward from `01-01-SUMMARY.md` — plan `01-05` reads it from here to
assert the closing commit series.
</output>

# Database
DATABASE_NAME=resume_strapi
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=change-me-in-production

# Strapi
APP_KEYS=key1,key2,key3,key4
API_TOKEN_SALT=generate-random-salt
ADMIN_JWT_SECRET=generate-random-secret
TRANSFER_TOKEN_SALT=generate-random-salt
JWT_SECRET=generate-random-secret

# Strapi API token (for Astro to fetch content)
STRAPI_API_TOKEN=create-read-only-token-in-strapi-admin

# Email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@ayoub-hidri.dev
EMAIL_REPLY_TO=ayoub.hidri@gmail.com

# Webhook
WEBHOOK_SECRET=generate-random-secret
